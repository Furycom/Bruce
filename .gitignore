# Backups
*.bak*
*.backup*
*_backup_*
server_backup_*

# Old patches
_patch_*/
_bak_*/
_scratch_*/
_gateway_backups/

# Environment
.env
.env.*
!.env.example

# Node
node_modules/
package-lock.json
__pycache__/

# Logs  
*.log
bruce-fallback.log

# Docker image artifacts
sha256:*

# Misc
*.tmp
*.swp
*.snip
mcp-gateway/
config/
patch_ssh_keypref.py

# Exception: track Dockerfile and entrypoint (but not node_modules etc)
!mcp-gateway/Dockerfile
!mcp-gateway/entrypoint.sh
!mcp-gateway/package.json
!mcp-gateway/connectors.json
*.broken*

# === ADDED BY SESSION 1243 - Git sync audit ===

# Model files (too large for Git)
*.gguf
*.ggml
*.bin
*.safetensors

# Secrets and tokens
*_key*
*secret*
*token*
cloudflared/config/

# DSPy cache and temp results
.dspy_cache/
dspy_results_*/
*.lock

# Large result directories (track summary JSON only)
bench_results/
bench_results_v2/
bench_results_v3/

# OS files
.DS_Store
Thumbs.db
